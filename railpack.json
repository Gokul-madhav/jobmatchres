{
  "provider": "python",
  "steps": {
    "install": {
      "commands": [
        "pip install --upgrade pip",
        "pip install ."
      ]
    }
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}